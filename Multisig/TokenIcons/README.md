Place local token icons in this folder.

Usage from token whitelist:
- Use only the filename in `image`, for example `usdt.png`.
- The app resolves it to `Bundle.main` subdirectory `TokenIcons/`.

Notes:
- Keep filenames lowercase to avoid case-sensitivity issues.
- Prefer square PNG icons (for example 64x64 or 128x128).
